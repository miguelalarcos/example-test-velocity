language: node_js
node_js:
  - "0.10"
git clone https://github.com/meteor/meteor.git ~/meteor
export PATH=~/meteor/:$PATH
npm install -g velocity-ci
npm install -g meteorite
mrt install velocity
mrt install mocha-web-velocity
mrt install velocity-html-reporter
mrt install moment

velocity
